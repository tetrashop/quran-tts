
مقاله علمی: شبیه‌سازی تلاوت قرآن با استفاده از مدل XTTS-v2

چکیده

در این پروژه از مدل متن‌باز XTTS-v2 برای تولید تلاوت قرآن با سبک یک قاری خاص استفاده شده است. تنها با چند ثانیه صدای مرجع، مدل می‌تواند هر آیه‌ای را با همان تُن، زیروبمی و ریتم تقلید کند. چالش‌های اصلی شامل اعراب‌گذاری دقیق، مدیریت تفاوت‌های تجویدی و محدودیت‌های سخت‌افزاری بود که با تنظیمات مناسب برطرف شدند.

مقدمه

تلاوت قرآن هنری دیرینه است و شبیه‌سازی آن با فناوری روز می‌تواند به آموزش و دسترسی‌پذیری کمک کند. مدل‌های TTS عصبی امروزی، مانند XTTS-v2، امکان کلونینگ صدا را بدون نیاز به آموزش مجدد فراهم می‌کنند.

روش کار

1. مدل: XTTS-v2 یک مدل تبدیل متن به گفتار چندزبانه است که از معماری Transformer و رمزگذار صوتی (vocoder) استفاده می‌کند. این مدل روی هزاران ساعت داده‌ی چندزبانه از پیش آموزش دیده است.
2. صدا ورودی: صدای مرجع (حداقل ۶ ثانیه) به یک بردار گوینده (speaker embedding) تبدیل می‌شود.
3. متن ورودی: آیه‌ی قرآن باید با اعراب کامل (تشکیل) نوشته شود تا تلفظ صحیح (تجوید) حاصل شود.
4. خروجی: گفتار ترکیب‌شده با حفظ سبک گوینده و ویژگی‌های صوتی او.

ارزیابی

· سرعت: روی GPU (NVIDIA T4) هر آیه حدود ۵-۱۰ ثانیه زمان می‌برد. روی CPU حدود ۲-۴ دقیقه.
· کیفیت: با اعراب‌گذاری صحیح، تلفظ حروف عربی و کشیدگی‌ها نسبتاً طبیعی است، اما گاهی در مصوت‌های کوتاه خطا دارد.
· محدودیت‌ها: مدل برای خواندن حرفه‌ای قرآن آموزش ندیده، بنابراین ممکن است قواعد تجویدی پیشرفته را رعایت نکند.

نتیجه‌گیری

پروژه نشان می‌دهد که با فناوری‌های متن‌باز می‌توان ابزاری مفید برای تولید تلاوت شخصی‌سازی‌شده ساخت. بهبودهای آینده می‌تواند شامل fine-tuning روی مجموعه‌داده‌های تخصصی قرآن و افزودن تنظیمات تجویدی باشد.

منابع

· Coqui TTS: https://github.com/coqui-ai/TTS
· مقاله‌ی XTTS: https://coqui.ai/blog/tts/open_xtts
  EOF

─── bugs_and_solutions.md ───

cat > bugs_and_solutions.md << 'EOF'

باگ‌ها و راه‌حل‌ها

۱. خطای ModuleNotFoundError: No module named 'torch'

علت: نصب نشدن کتابخانه‌های ضروری.
راه‌حل: مطمئن شوید pip install -r requirements.txt را به‌درستی اجرا کرده‌اید و پایتون نسخه‌ی ۳.۹ تا ۳.۱۱ دارید.

۲. خطای Could not find a version that satisfies the requirement TTS

علت: نسخه‌ی پایتون شما ۳.۱۳ یا بالاتر است (TTS حداکثر تا ۳.۱۱ پشتیبانی می‌کند).
راه‌حل: از پایتون ۳.۹، ۳.۱۰ یا ۳.۱۱ استفاده کنید. می‌توانید با pyenv یا conda نسخه را مدیریت کنید.

۳. اجرا در Termux / اندروید

علت: کتابخانه‌های PyTorch و TTS برای معماری ARM کامپایل نشده‌اند و حتی در صورت نصب، با خطا مواجه می‌شوید.
راه‌حل: از محیط‌های ابری مانند Google Colab یا Hugging Face Spaces استفاده کنید. پروژه برای این پلتفرم‌ها بهینه شده است.

۴. خطای TypeError: tts() got an unexpected keyword argument 'sample_rate'

علت: در نسخه‌های اولیه کد، پارامتر sample_rate به tts.tts ارسال می‌شد که وجود ندارد.
راه‌حل: در نسخه‌ی نهایی این پارامتر حذف شده است. کد فعلی را جایگزین کنید.

۵. خروجی صوتی بی‌کیفیت یا نویز

علت: فایل مرجع استریو، نویز پس‌زمینه، یا متن بدون اعراب کامل.
راه‌حل:

· فایل صوتی را به mono تبدیل کنید (کد به‌طور خودکار این کار را می‌کند).
· از نمونه‌ی تمیز و بدون نویز استفاده کنید.
· متن آیه حتماً دارای تمام حرکات (اعراب) باشد.

۶. خطای حافظه (GPU OOM)

علت: مدل XTTS حدود ۲ گیگابایت حافظه GPU نیاز دارد. اگر همزمان چند درخواست بیاید، ممکن است کمبود حافظه رخ دهد.
راه‌حل: از torch.cuda.empty_cache() استفاده کنید یا روی CPU اجرا کنید (کندتر اما مطمئن‌تر).
